{
  "name": "xtc-toaster",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "sandbox": "pnpm --filter @xtc-toaster/sandbox dev",
    "sandbox:build": "pnpm --filter @xtc-toaster/sandbox build",
    "api": "pnpm --filter @xtc-toaster/api dev",
    "web": "pnpm --filter @xtc-toaster/web dev"
  }
}
