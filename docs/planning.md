- [ ] web-интерфейс будет только для предпросмотра эффектов и прогона собственных изображений по эффектам
- [ ] стили веб-интерфейса забазируй из этого файла - https://github.com/iwwwanow/owo
  - стили встраиваем напрямую в web-пакет (без отдельного ui-kit)
- [ ] cli-интерфейс позволяет публиковать эффекты (давай так их называть)
  - для этого нужно передвать `name`, `description (optional)`, `preview (gif)`
  - авторизация через логин/пароль (хранится в api, простая basic/token auth)
- [ ] все это должно сохраняться в бд sqlite
- [ ] также подготовь экшены для публикации образов для сервисов
- [ ] docker compose для dev-режима
- [ ] тоесть на выходе должно быть 5 пакетов:
  - core - typescript processing logic (собствено сам canvas-playground)
  - sandbox - svelte dev-песочница (текущий src/)
  - web (go, с испольованием стилей из owo) - для просмотра пресетов и прогона своих изображений по пресетам
  - cli - node - для публикации собственных пресетов
  - api (nodejs, hono) - для просмотра эффектов и использования cli в других бэкэндах
- [ ] подготовь github-actions для сборки и публикации новых образов

- сборник заскриптованных фильтров
- сборник генераторов графики
- каждый фильтр / генератор будет называться `toast`
- запечь тост значит отрендерить
- каждый тост возвращает изображение
